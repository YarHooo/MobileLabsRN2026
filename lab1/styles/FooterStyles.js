import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    textAlign: 'center',
    fontStyle: 'italic',
    margin: 8,
    color: 'gray',
  },
});

export default styles;
