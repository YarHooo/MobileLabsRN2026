import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    flex: 1,
  },
  container: {
    padding: 10,
  },
  box: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#eee',
    width: 150,
    height: 100,
    margin: 8,
    borderRadius: 8,
  },
});

export default styles;
